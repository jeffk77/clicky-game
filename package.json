{
  "homepage": "https://jeffk77.github.io/clicky-game",
  "name": "clicky-game",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.6.0",
    "react-dom": "^16.5.0",
    "react-scripts": "1.1.5"
  },
  "devDependencies": {
    "gh-pages": "^2.0.0"
  },

  "scripts": {
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
